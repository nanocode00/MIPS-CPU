{
    /*"actionButtons": {
        "defaultColor": "#ffffff",
        "reloadButton": "↻",
        "commands": [
            {
                "name": "$(play) Run Verilog",
                "color": "white",
                "command": "iverilog -o ${fileBasenameNoExtension}.out ${file} && vvp ${fileBasenameNoExtension}.out"
            },
            {
                "name": "$(graph) Run Verilog + GTKWave",
                "color": "white",
                "command": "iverilog -o ${fileBasenameNoExtension}.out ${file} && vvp ${fileBasenameNoExtension}.out && gtkwave wave.vcd"
            }
        ]
    },*/

    "VsCodeTaskButtons.showCounter": true,
    "VsCodeTaskButtons.tasks": [
        {
            "label": "$(play) Run Verilog (current file)",
            "task": "Run Verilog (current file)",
            "tooltip": "Run Verilog Simulation"
        },
        {
            "label": "$(graph) Run Verilog + GTKWave (current file)",
            "task": "Run Verilog + GTKWave (current file)",
            "tooltip": "Run Verilog Simulation + GTKWave Waveform Viewer"
        }
    ]
}